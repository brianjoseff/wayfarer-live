%h1 Journals

-if current_user && current_user.admin?
  =link_to "Add Journal entry", new_journal_entry_path
  %br/
  %br/
  / %table
  /   %tr
  /     %th Content
  /     %th
  /     %th
  /     %th
  / 
  /   - @journal_entries.each do |journal_entry|
  /     %tr
  /       %td= journal_entry.content
  /       %td= link_to 'Show', journal_entry
  /       %td= link_to 'Edit', edit_journal_entry_path(journal_entry)
  /       %td= link_to 'Destroy', journal_entry, :method => :delete, :data => { :confirm => 'Are you sure?' }


=render @journal_entries




